# Что из заявленного работает — 2026-09-25

Обновление к `FUNCTIONAL_STATUS_2026-09-15.md`. Тогда шесть функций упирались
в утраченный `server.mjs`. Шлюз написан заново; ниже — что это изменило и что
осталось непроверенным.

Где стоит «проверено» — есть команда или прогон, вывод которого приведён. Где
«не проверено» — я этого не запускал и не утверждаю.

---

## Что изменилось

`server.mjs` не восстановлен из архива — он написан заново по контракту,
который клиент уже диктовал. Это было возможно потому, что серверная **логика**
пережила загрузку в четырёх модулях в корне репозитория, а потерялась только
HTTP-обвязка:

| Модуль | Что в нём было | Строк |
|---|---:|---:|
| `account-auth-core.mjs` | scrypt-записи паролей, токены сессий, сравнение постоянного времени | 83 |
| `media-connectors.mjs` | разбор субтитров YouTube, нормализация расшифровок, разбиение OCR по страницам | 113 |
| `sync-core.mjs` | формат снимка и его валидация | 28 |
| `instagram-core.mjs` | разбор ссылок и нормализация поста | 82 |

| | Функций | Было 15.09 | Стало 25.09 |
|---|---:|---|---|
| Работает без шлюза | 9 | 9 | 9 |
| Работает через подключённую модель | 4 | 4 | 4 |
| Требовало утраченного шлюза | 6 | **не работало** | **шлюз есть** |
| Кода нет вообще | 2 | 2 | **1** (остался Android) |

---

## Шесть функций, которые были заблокированы

| Функция | Маршрут | Состояние |
|---|---|---|
| Загрузка по URL с защитой от SSRF | `POST /api/source-url` | **работает, проверено** |
| Аккаунты и устройства | `/api/account/*` | **работает, проверено** |
| Синхронизация рабочего пространства | `/api/account/sync/:id`, `/api/sync/:id` | **работает, проверено** |
| Расшифровка аудио и видео | `POST /api/transcribe`, `/api/uploads/*` | **работает против мок-провайдера**; на живом Whisper не проверял |
| OCR и разбор изображений | `POST /api/vision` | **работает против мок-провайдера**; на живой vision-модели не проверял |
| YouTube-субтитры | `POST /api/youtube` | **логика проверена на заглушке; против живого YouTube НЕ проверял** — см. оговорки |
| Instagram | `POST /api/instagram` | **мост написан и проверен; сборщика в комплекте нет** — см. оговорки |

## Чем подтверждено

```
npm test          → # tests 201  # pass 201  # fail 0      (было 50 на 14.09)
npm run build     → ✓ built in 992ms
npm start         → NoteAI gateway слушает http://0.0.0.0:8787
```

Сквозной прогон в Chromium: собранное приложение, которое отдаёт сам шлюз,
обращается к `/api/ai`, шлюз идёт к OpenAI-совместимому провайдеру и
возвращает его ответ.

```
gateway serving the built app at http://127.0.0.1:43913/
app title           : NOTE2
health.capabilities : {"sourceUrl":true,"youtube":true,"ai":true,"embed":false,
                       "vision":false,"transcribe":false,"instagram":false,"legacySync":false}
POST /api/ai        : 200 application/json; charset=utf-8
AI OUTPUT           : ОТВЕТ МОДЕЛИ ЧЕРЕЗ ШЛЮЗ. Системная инструкция получена: true
provider was called : 1 time(s)
unknown /api/ route : 404 application/json; charset=utf-8 {"error":"Неизвестный маршрут ..."}
horizontal overflow : 0 px
page errors         : none
```

Последние две строки — про два отдельных дефекта: промах по `/api/` отвечает
JSON-ошибкой, а не страницей приложения (иначе пользователь видит
`Unexpected token '<'`), и приложение не вылезает за ширину экрана 390 px.

## Правило, которое соблюдается во всех маршрутах модели

Ненастроенная возможность отвечает **503 и называет переменную**, которая её
включит. Она не возвращает ни `output`, ни `text`, ни `vectors`. Локально
извлечённый текст, отданный отсюда, дошёл бы до клиента с пометкой
`origin: 'model'` и был бы неотличим от настоящего анализа — это единственный
вид отказа, который ниже по течению обнаружить нечем.

Отдельный тест проверяет все четыре маршрута сразу и падает, если хоть один
вернёт текст без настроенной модели.

## Оговорки — что я НЕ проверял

1. **YouTube против живого сайта.** Сетевая политика этого окружения запрещает
   CONNECT к `www.youtube.com` (в логе прокси: `gateway answered 403 to
   CONNECT`). Разбор проверен на заглушке страницы. Нужно подтвердить, что
   страница видео по-прежнему содержит `captionTracks` в том виде, который
   ожидает `media-connectors.mjs`.
2. **Живые модели.** Все маршруты моделей проверены против мок-провайдера,
   который записывает присланное. Настоящих OpenAI, Ollama или Whisper я не
   вызывал — ключей в окружении нет.
3. **Instagram.** Мост проверен полностью: запуск команды, таймаут, ненулевой
   код возврата, отказ до запуска на не-Instagram ссылке. Самого сборщика
   репозиторий не содержит и не обходит ограничения площадки.
4. **Устройства не было.** safe-area, Share Target, Keystore подтверждены
   только кодом.
5. **Миграции IndexedDB v1→v3** на реальных старых данных не проверялись.
   `source-db.js`, `source-index.js`, `job-core.js` и `instagram-analysis.js`
   остаются реконструкцией по контрактам вызовов, а не оригиналами.

## Что осталось

**Android.** Каталога `android/` нет, `scripts/prepare-android.mjs` нет.
`capacitor.config.json` на месте, `appId` — `app.noteai.workspace`. Это
единственный пункт, где кода нет вообще.

**Покрытие клиента.** 201 теста — это шлюз, восстановление архива и несколько
клиентских модулей. Исходные 39 тестов приложения утрачены; заметок,
источников, поиска и миграций IndexedDB покрытия по-прежнему нет.
